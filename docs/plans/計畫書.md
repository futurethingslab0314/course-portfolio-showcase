# GenericCard 視覺升級與資料串接計畫書

版本：v1  
日期：2026-03-01

## 1. 計畫目標

1. 依 `GENERIC_CARD_CHANGES.md` 落地 GenericCard 視覺升級（Slider、滿版視覺、新版 Modal 排版）。
2. 保持 `generic-card` 為穩定 fallback，不影響其他 `UiPattern`（`gallery-*`, `card-spec`, `data-matrix`）。
3. 以彈性資料層支援不同來源資料庫欄位命名，最終都正規化到 `StudentWork`。
4. 建立可驗證的交付標準（UI、資料完整性、回退機制、測試）。

## 2. 目前狀態與差距

### 2.1 依據文件的目標樣式

`GENERIC_CARD_CHANGES.md` 定義了以下方向：
1. Modal 內建圖片 Slider（主圖 + moreImages）。
2. 圖片區塊改為滿版寬螢幕（`aspect-video` / `21:9`）。
3. 左右切換按鈕、底部 dot indicator、鍵盤操作（左右鍵、Esc）。
4. 更大的容器與更清楚的資訊分欄（描述 8 欄、成員 4 欄）。

### 2.2 現況程式碼差異

`src/components/projects/GenericCard.tsx` 目前仍是舊版行為：
1. Modal 只有主圖，沒有 Slider 狀態管理。
2. 圖片比例仍偏方形，不是沉浸式滿版寬螢幕。
3. 尚未實作左右鍵切換與 dot indicator。
4. 內容欄位顯示固定，尚未抽象成「可彈性帶資料」的資訊區塊配置。

## 3. 視覺更新策略（GenericCard UI）

### Phase A：元件結構重組

1. 將 `GenericCard` 拆成三層：
   - `GenericCardPreview`：列表卡片縮圖。
   - `GenericCardModal`：彈窗容器與關閉流程。
   - `GenericCardSlider`：圖片播放、切換、指示器。
2. 保留現有 `memberRows(work)` 作為成員正規化入口，避免破壞舊資料。

### Phase B：Slider 與互動

1. 圖片來源統一：`images = [mainImage, ...moreImages]`（去重、過濾空值）。
2. 狀態管理：`currentImageIndex`、`isModalOpen`。
3. 互動：
   - Click 左右按鈕切換。
   - 鍵盤 `ArrowLeft/ArrowRight/Escape`。
   - Dot indicator 可點擊跳轉。
4. 動畫：延續 `AnimatePresence mode="wait"`，統一進出場 easing 與 duration。

### Phase C：新版版面與資訊區塊

1. Modal 容器升級為 `max-w-6xl`，桌機圓角、行動裝置滿高。
2. 上半部使用滿版視覺區塊，桌機採 21:9，手機退回 `aspect-video`。
3. 下半部文字排版：
   - 左側：`assignmentName` + `description` + tags/year。
   - 右側：members（可顯示 studentId）。
4. 無資料時使用安全降級：
   - 無 `year` 時顯示 `TBD`。
   - 無 `members` 時顯示 `Unassigned`。

## 4. 資料庫彈性串接策略

### 4.1 資料正規化原則

以 `shared/notionMapper.ts -> normalizeStudentWork()` 為唯一入口，所有來源 DB 欄位先轉成 `StudentWork` 再渲染 UI。

1. UI 只依賴 `StudentWork`，不直接讀取來源原始欄位。
2. `FieldMapping` 可 JSON 或 line-based，支援 `sourceCandidates + transform + default`。
3. `generic-card` 不要求特殊欄位，但建議補 `moreImages/year/tags` 供升級版 UI 使用。

### 4.2 GenericCard 必要與建議欄位

1. 必要欄位（最低可渲染）：
   - `assignmentName`, `members`, `description`, `mainImage`, `sourceDatabaseId`
2. 建議欄位（提升體驗）：
   - `moreImages`：啟用 Slider 體驗
   - `year`, `tags`：補齊 metadata
   - `studentIds`：成員識別

### 4.3 防呆與回退

1. `UiPattern` 缺漏/非法 -> fallback `generic-card`（現有 `mapUiPattern` 已支援）。
2. `mainImage` 缺漏 -> fallback 圖（現有 normalize 已支援）。
3. `moreImages`、`tags`、`members` 透過 `ensureStringArray` 保證型別安全。
4. 來源欄位名稱混亂時，靠 `sourceCandidates` 多候選與 alias 規則吸收差異。

## 5. 資料結構擴充提案（保持相容）

為了讓 GenericCard 視覺更可配置，建議新增「可選」UI metadata（不破壞既有欄位）：

```ts
interface StudentWorkUiMeta {
  cardVariant?: 'default' | 'immersive';
  heroAspect?: 'square' | 'video' | 'cinematic';
  badgeLabel?: string;
  showTeamIds?: boolean;
}
```

實作方式：
1. 可先掛在 `StudentWork` 的 `uiMeta?: StudentWorkUiMeta`（選填）。
2. `FieldMapping` 不需要強制提供；缺值時由前端 default。
3. 若來源 DB 未提供，GenericCard 行為維持現況，不會破壞舊資料。

## 6. 實作步驟與時程（建議）

### Day 1：UI 重構

1. 拆分 `GenericCard` 子元件。
2. 完成 Slider 狀態、按鈕、指示器、鍵盤事件。
3. 完成滿版視覺比例與新版 Modal 排版。

### Day 2：資料串接與相容

1. 盤點並補齊 `FieldMapping` 建議候選（`moreImages`, `year`, `tags`, `studentIds`）。
2. 新增 `uiMeta` 選填 schema（若採納）。
3. 補 fallback 與空值顯示策略。

### Day 3：驗證與上線準備

1. 測試 5 種 `UiPattern` 不回歸。
2. 驗證不同來源欄位命名下 GenericCard 渲染穩定。
3. 整理更新文件（`docs/ui-pattern-json-spec.md` + 範例 mapping）。

## 7. 驗收標準

1. GenericCard modal 可切換多圖，且支援滑鼠、鍵盤、點擊指示器。
2. 桌機/手機視覺比例正確（桌機 21:9、手機 video）。
3. 來源 DB 欄位命名不同時，經 `FieldMapping` 後仍能正常渲染。
4. `UiPattern` 缺漏或錯誤時，仍可落在 `generic-card` 並正常展示。
5. 無 `moreImages/year/studentIds` 時，UI 無錯誤且有合理降級顯示。

## 8. 風險與緩解

1. 風險：動畫/鍵盤事件造成 modal 焦點問題。  
   緩解：加入 focus 管理與 Esc 全域關閉測試。
2. 風險：來源 DB 欄位命名漂移導致 mapping 漏接。  
   緩解：維護 alias 字典與 confidence review 流程。
3. 風險：新增 `uiMeta` 造成型別擴張成本。  
   緩解：先選填 + default，分階段導入，不阻斷主流程。

## 9. 交付物

1. `src/components/projects/GenericCard.tsx`（升級版結構）。
2. `src/styles/cards/card-styles.css`（GenericCard 專屬樣式增補）。
3. `shared/notionMapper.ts` / `server/services/mappingPipeline.ts`（必要時補 mapping 規則）。
4. 文件更新：`docs/ui-pattern-json-spec.md`、`docs/source-database-schema-guideline.md`（若欄位策略有新增）。
